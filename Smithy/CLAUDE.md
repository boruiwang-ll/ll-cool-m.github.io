# Agent Notes

`Smithy/` is a private knowledge-keeping workspace and is excluded from Jekyll builds.

Each article topic uses this structure:

- `ore/`: collected links, media, files, and references
- `forge/`: notes, thoughts, diagrams, and working drafts
- `hallmark/`: publish-ready posts for the blog
